// Pages
export { default as AdminPage } from "./admin/pages/AdminPage";

// Admin Components
export { default as ContractList } from "./admin/components/ContractList";
export { default as ContractForm } from "./admin/components/ContractForm";

// Public Components

// Types (if any)
export * from "./types";

// Lokales
export { default as translations } from "./locales";

// Optionally: i18n can be loaded in page-level or via config
