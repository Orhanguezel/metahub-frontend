// Pages
export { default as AdminPage } from "./admin/pages/AdminPage";

// Admin Components
export { default as ContactList } from "./admin/components/ContactList";
export { default as ContactForm } from "./admin/components/ContactForm";

// Public Components

// Types (if any)
export * from "./types";

// Lokales
export { default as translations } from "./locales";

// Optionally: i18n can be loaded in page-level or via config
